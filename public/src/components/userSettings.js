import {onChange} from "../utils";

export default function UserSettings() {
    return (
            <Fragment>
                {/*BLZ: <input value={blz} onChange={onChange(setBlz)}/><br/>*/}
                {/*User: <input value={user} onChange={onChange(setUser)}/><br/>*/}
                {/*PIN: <input type="password" value={pin} onChange={onChange(setPin)}/><br/>*/}
                {/*FINTSURL: <input value={fintsUrl} onChange={onChange(setFintsUrl)}/><br/>*/}
            </Fragment>
    )
}
